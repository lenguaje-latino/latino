/*
Para debuguear en visual studio:
Menu propiedades del proyecto-> Debugging -> Command Arguments. Agregar
$(SolutionDir)..\ejemplos\debug.lat
*/

funcion crear_archivo_ids_usuarios()
  llave = [1,2,3,4,5]
  //json_ = {"usuarios": []}
  lst = []
  desde(i=0; i<llave; i++)
    //lista.agregar(json_.usuarios, { i : llave[i]})
    lista.agregar(lst, { i : llave[i]})
  fin
  //imprimir(json_)
  imprimir(lst)
fin

crear_archivo_ids_usuarios()
