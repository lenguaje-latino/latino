funcion factorial(n)
    #resultado como double
    x = 1.0
    desde i=2 hasta (n + 1)
        x = x * i
        #imprimir("i:"  + i + ", x:"  + x)
    fin
    retorno x
fin

imprimir("el factorial de 10 es: " + factorial(1))
