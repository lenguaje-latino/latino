/*
Para debuguear en visual studio:
Menu propiedades del proyecto-> Debugging -> Command Arguments. Agregar
$(SolutionDir)..\ejemplos\debug.lat
*/

str = "esta cadena
tiene saltos 
de linea"

str2 = "esta cadena\n tiene saltos\n de linea"

mod = reemplazar(str, "\n", "-")
escribir(str)
escribir(mod)
mod = reemplazar(str2, "\\n", "-")
escribir(mod)
mod = reemplazar(str2, "cadena", "string")
escribir(mod)
