funcion avg(a,b)
    #imprimir((a+b)/2.0)
    x = (a+b)/2.0
    imprimir(x)
fin

funcion max(a,b)
    si (a >= b)
        imprimir(a)
    sino
        imprimir(b)
    fin
fin

avg(3, 5)
#error muy pocos argumentos
max(3)

funcion fun()
   #empty function
   #imprimir("funcion indefinida")
fin

funcion fun_1(a)
    imprimir(a)
fin

#TODO: error al llamar a funcion sin parametros
fun(0)
#TODO: no evia error cuando se pasan mas argumentos
fun_1(100, 2, 1)
